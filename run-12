host="$(hostname)"
name="${NAME:-$host}"
if [ "$1" == "nolive" ]; then
  live=""; shift
else
  live="--live"
fi
./run-client $live --name "$name" --redis --feed --feed-host 192.168.1.141 --feed-port 4714 "$@"
